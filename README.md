This is a Bun workspaces monorepo. The API lives in `apps/api`.

To install dependencies (run from the repo root):

```sh
bun install
```

To run the dev server (fans out to each app's `dev` script):

```sh
bun run dev
```

open http://localhost:3000
