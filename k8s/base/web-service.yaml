apiVersion: v1
kind: Service
metadata:
  name: shurl-web
  labels:
    app.kubernetes.io/name: shurl-web
    app.kubernetes.io/part-of: shurl
spec:
  selector:
    app.kubernetes.io/name: shurl-web
  ports:
    - name: http
      port: 80
      targetPort: http
