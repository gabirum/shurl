# Copy to api.secret.env (gitignored) and fill in real values before `kubectl apply -k`.
# MySQL is deployed externally (managed instance / existing server) — not part of these
# manifests — so only its connection string is needed here.
DATABASE_URL=mysql://shurl:CHANGE_ME@mysql.example.internal:3306/shurl
REDIS_URL=redis://redis:6379
